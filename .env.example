NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id_here
NEXT_PUBLIC_SANITY_DATASET=production

# Google Apps Script Web App URL backing /volunteer (the ".../exec" URL from
# Deploy > New deployment > Web app on the volunteer Google Sheet's bound
# script). Server-only -- deliberately NOT prefixed NEXT_PUBLIC_, since this
# endpoint can write/decrement live sign-up data.
VOLUNTEER_APPS_SCRIPT_URL=
